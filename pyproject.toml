[project]
name = "todoist-tool"
version = "0.1.0"
description = "Todoist MCP server and management scripts"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]",
    "requests",
    "python-dotenv",
]
